# 設計方針決定サマリー

これまでの対話で決定した設計方針のまとめです。詳細な検討過程・比較検討した他の選択肢は
[選定要素提案.md](./選定要素提案.md)、ライブラリの具体的なバージョンは[技術スタック.md](./技術スタック.md)、
DB/API/画面まで含めた設計の詳細は[docs/manifest.md](docs/manifest.md)を参照してください。

---

* **リポジトリ構成**: 単一リポジトリ(モノレポ)。pnpm workspace + turboで`backend`/`frontend`/`packages/shared`を管理。バージョン管理はGit(ローカルで`git init`済み、初回コミットは未実施)。納品時のGitHubへのアップロードはユーザー側で行う
* **絶対要件の技術スタック**（TypeScript / React / Express / Prisma / PostgreSQL）はすべて充足済み。「Express」はNestJSが内部で`@nestjs/platform-express`経由で使用していることをもって満たすと判断（ユーザー確認済み）
* **アーキテクチャ**: 前プロジェクト(uniteboard)と同じSPA(React+Vite) + REST API(NestJS+Prisma+PostgreSQL)構成をそのまま継続。BFF/SSRは採用せず、リアルタイム同期(WebSocket等)も導入しない（操作後にTanStack Queryのキャッシュ無効化で最新化する）
* **提出形態**: GitHubへのコードアップロードのみ。デプロイ・ホスティングは不要で、`docker compose up`一発でdb/backend/frontendが起動すればOK。CI/CDは導入しない
* **推奨(Should)機能のスコープ**: 要件定義の推奨機能のうち、**キャンセル待ち＋自動繰り上げ**と**フィードバック(星評価＋コメント)は実装対象に含める**。**通知システムのみ実装対象から除外**する
* **認証・セッション管理**: Access+Refresh Token(JWT)をhttpOnly Cookieで管理する方式を継続。Access Tokenのペイロードは`role`を含めず`sub`(userId)のみとし、毎リクエストDBから最新のロール・有効/無効状態を引き直す方式に**決定**（要件文言は「ロールをトークンに含める」だが、昇格/降格・無効化を即時反映できる現行方式を優先。ユーザーに確認済み・特にこだわりなしとの回答のため推奨案を採用）
  * Refresh Tokenのローテーション・DB失効管理は実装済み。「使い回し(リプレイ)検知時に全セッションを強制失効させる」機能の追加も同様に決定済みだが、コード自体は未実装（着手時に追加予定）
* **主催者権限**: 独立ロールを設けず、`Event.organizerId`と実行者本人の一致をCASLでその都度判定する方式
* **カテゴリ削除**: 紐づくイベントが1件でもあれば物理削除を拒否(`409`)。論理削除・別カテゴリへの自動移動は採用しない
* **イベント削除**: 論理削除(`deletedAt`)。参加履歴・出席率・フィードバックの整合性を保つため
* **参加登録**: 主催者は暗黙的に参加確定とし、`Registration`レコードを作らない。API操作は「登録」「キャンセル」の2アクションのみとし、満席時のキャンセル待ちは登録アクションが自動判定する
* **キャンセル待ちの順序**: FIFO方式（新規待機は常に列の末尾=`現在の最大position + 1`）。挿入位置の中間値採番方式は当初案から訂正済み
* **繰り上げ処理**: イベント行のロック(`SELECT ... FOR UPDATE`)を用いたトランザクションで実装。繰り上げ履歴は`PromotionHistory`テーブルに保存(将来の通知機能への布石)
* **出席率**: 未マークをnull許容にし、集計時に分母から除外
* **フィードバック**: 開催終了済み＋出席済み(`ATTENDED`)のユーザーのみ星評価(1〜5)＋コメントを投稿可。`(eventId, userId)`の一意制約で1人1件に制限(編集は可)。匿名投稿(`isAnonymous`)は投稿者情報を一般ユーザーに非表示・adminには表示。adminによる非公開化(`isHidden`)は物理削除ではなく集計除外で対応
* **通知システム**: 今回のスコープでは実装しない（要件でも送信までは不要と明記されているため）
* **セキュリティ**: XSSはReactの自動エスケープに全面依拠(サニタイズライブラリ不使用、`dangerouslySetInnerHTML`不使用)。SQLインジェクションはPrismaのパラメータ化クエリに依拠。パストラバーサルはファイルアップロード機能自体が要件にないため対応不要。CORSはOrigin限定+`SameSite=Lax`、レート制限はログイン試行回数のみ
* **API設計**: REST継続、専用アクションエンドポイント方式(`POST /events/:id/register`等)。参加登録ボタンの状態(`registrationState`)はバックエンドで計算しフロントは表示に徹する。エラー形式・ページネーション方針は既存実装を踏襲
* **タグ**: 完全自由入力+trim/小文字化程度の軽い正規化のみ。タグマスタ化はしない
* **日時**: DBはUTC(`@db.Timestamptz`)保存、表示はdayjsでJST変換
* **コーディング規約**: [docs/coding_standards.md](docs/coding_standards.md)に確定。MANIFEST.mdの技術スタック・ディレクトリ構成に沿って全面改訂済み（命名規則・レイヤー構成・Prisma規約・API設計・テスト・Git・環境変数・セキュリティ・レビューチェックリスト）

## 現状のコード実装状況

* ✅ 実装済み: 認証基盤一式(ログイン/ログアウト/リフレッシュ、CASL、共通UI・ルーティング)。前プロジェクトからの継承+新規ドメイン用に調整済み
* ❌ 未実装: イベント・カテゴリ・参加登録・出席・フィードバック関連のコード全般（Prismaスキーマも`User`/`RefreshToken`のみ。[docs/manifest.md](docs/manifest.md)は設計書止まり）
