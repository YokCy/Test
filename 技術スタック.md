# 技術スタック

[選定要素提案.md](./選定要素提案.md)で決定した方針を実現するための、具体的なライブラリ・バージョンの一覧。
バージョンは`pnpm list --depth=0`で実際に解決されたもの(2026-09-01時点)を記載する
(`package.json`上は`^`によるレンジ指定のため、実際にインストールされるバージョンは今後の
インストールタイミングで多少前後しうる)。

---

## 実行環境

| 項目 | バージョン |
|---|---|
| Node.js | v22.15.1 |
| pnpm | 9.15.0 |
| Docker | 29.7.2 |
| PostgreSQL(イメージ) | `postgres:16` |

---

## バージョン管理

| ツール | バージョン | 用途 |
|---|---|---|
| Git | 2.53.0 | ソースコードのバージョン管理。単一リポジトリで`backend`/`frontend`/`packages/shared`をまとめて管理する |

納品時のGitHubへのアップロードはユーザー側で行う(2026-09-01確認済み)。

---

## モノレポ管理

「1つのリポジトリ内で複数パッケージをどう繋いで一括ビルド・実行するか」の管理方式。バージョン管理(Git)とは別の関心事。

| ライブラリ | バージョン | 用途 |
|---|---|---|
| pnpm workspace | (pnpm 9.15.0に同梱) | `backend`/`frontend`/`packages/shared`の依存関係・ビルドをワークスペースとして一元管理 |
| turbo | ^2.3.0 | `dev`/`build`/`lint`/`test`をワークスペース横断で実行するタスクランナー |

---

## バックエンド(`backend/`)

### フレームワーク・コア

| ライブラリ | バージョン | 用途 |
|---|---|---|
| typescript | 5.9.3 | 言語 |
| @nestjs/core | 10.4.22 | アプリケーションフレームワーク本体 |
| @nestjs/common | 10.4.22 | Controller/Module/Guard等の共通デコレータ・ユーティリティ |
| @nestjs/platform-express | 10.4.22 | HTTPアダプタ(内部でExpressを使用。絶対要件「Express」を満たす根拠) |
| @nestjs/config | 3.3.0 | 環境変数の読み込み・起動時バリデーション(`ConfigModule`) |
| reflect-metadata | 0.2.2 | NestJSのデコレータ・DIが利用するメタデータ反射 |
| rxjs | 7.8.2 | NestJS内部（Interceptor等）が依存するリアクティブストリーム |

### データベース・ORM

| ライブラリ | バージョン | 用途 |
|---|---|---|
| @prisma/client | 5.22.0 | 型安全なDBクライアント |
| prisma | 5.22.0(devDependency) | スキーマ管理・マイグレーション・Prisma Client生成用CLI |

### 認証・認可

| ライブラリ | バージョン | 用途 |
|---|---|---|
| @nestjs/jwt | 10.2.0 | JWT(Access/Refresh Token)の署名・検証 |
| @nestjs/passport | 10.0.3 | NestJSでのPassport戦略統合 |
| passport | 0.7.0 | 認証ミドルウェア基盤 |
| passport-jwt | 4.0.1 | Cookie内JWTを検証するPassport戦略(`JwtStrategy`) |
| bcrypt | 5.1.1 | パスワードハッシュ化 |
| cookie-parser | 1.4.7 | httpOnly CookieからのJWT読み取り |
| @casl/ability | 6.8.1 | ロール・所有関係ベースの認可(CASL) |

### バリデーション

| ライブラリ | バージョン | 用途 |
|---|---|---|
| zod | 3.25.76 | スキーマバリデーション(`packages/shared`でフロントと共有) |
| nestjs-zod | 5.5.0 | ZodスキーマからNestJSのDTO(`createZodDto`)を生成、`ZodValidationPipe`でグローバル検証 |
| class-validator | 0.14.4 | NestJS標準のデコレータ検証(一部レガシー箇所・NestJS内部機構向け) |
| class-transformer | 0.5.1 | DTOのシリアライズ/デシリアライズ |

### テスト・Lint

| ライブラリ | バージョン | 用途 |
|---|---|---|
| jest | 29.7.0 | ユニットテスト |
| ts-jest | ^29.2.5 | JestでのTypeScriptトランスパイル |
| @nestjs/testing | 10.4.22 | NestJSのテスト用モジュールビルダー |
| eslint | ^8.57.0 | Lint |
| prettier | ^3.3.3 | フォーマッタ |
| ts-node | 10.9.2 | `prisma db seed`実行用(TypeScriptを直接実行) |

---

## フロントエンド(`frontend/`)

### フレームワーク・コア

| ライブラリ | バージョン | 用途 |
|---|---|---|
| typescript | 5.9.3 | 言語 |
| react | 18.3.1 | UIフレームワーク |
| react-dom | 18.3.1 | ReactのDOMレンダラー |
| vite | 5.4.21 | ビルドツール・開発サーバー |
| @vitejs/plugin-react | ^4.3.4 | ViteでのReact(JSX/Fast Refresh)サポート |

### ルーティング・状態管理

| ライブラリ | バージョン | 用途 |
|---|---|---|
| react-router-dom | 6.30.6 | クライアントサイドルーティング |
| @tanstack/react-query | 5.102.8 | サーバー状態のフェッチ・キャッシュ管理 |

### フォーム・バリデーション

| ライブラリ | バージョン | 用途 |
|---|---|---|
| react-hook-form | 7.87.0 | フォーム状態管理 |
| @hookform/resolvers | 3.10.0 | React Hook FormとZodスキーマの連携 |
| zod | 3.25.76 | バリデーション(`packages/shared`経由でバックエンドと共有) |

### スタイリング・UI

| ライブラリ | バージョン | 用途 |
|---|---|---|
| tailwindcss | 3.4.19 | ユーティリティファーストCSS |
| autoprefixer | ^10.4.20 | Tailwind用ベンダープレフィックス自動付与 |
| postcss | ^8.4.49 | Tailwindのビルドパイプライン |
| class-variance-authority | 0.7.1 | Tailwindクラスのバリアント管理(ボタン等のスタイルパターン) |
| dayjs | 1.11.23 | 日時フォーマット・タイムゾーン変換(UTC→JST表示) |

### テスト

| ライブラリ | バージョン | 用途 |
|---|---|---|
| vitest | 2.1.9 | ユニットテスト(Viteネイティブ) |
| @testing-library/react | 16.3.3 | Reactコンポーネントのテスト |
| @testing-library/user-event | 14.6.6 | ユーザー操作のシミュレーション |
| @testing-library/jest-dom | 6.10.0 | DOM向けの追加アサーション |
| @playwright/test | 1.62.1 | E2Eテスト |

---

## 共有パッケージ(`packages/shared/`)

| ライブラリ | バージョン | 用途 |
|---|---|---|
| zod | 3.25.76 | フロント・バック共有のバリデーションスキーマ・型定義の実体 |
| typescript | 5.9.3(devDependency) | 型定義のビルド(`tsc`) |

---

## インフラ・開発環境

| 項目 | 内容 |
|---|---|
| コンテナ化 | Docker Compose(`db`/`backend`/`frontend`の3サービス、[docker-compose.yml](./docker-compose.yml)) |
| DBイメージ | `postgres:16` |
| backend/frontendベースイメージ | `node:22-slim`(Prismaのクエリエンジンが要求するOpenSSL 1.1系の都合上、`alpine`ではなく`slim`を採用。理由は[backend/Dockerfile](./backend/Dockerfile)参照) |
| ローカルDBポート | ホスト`5433` → コンテナ内`5432`(ローカルPostgreSQLサービスとの competing を避けるため) |

---

## 意図的に採用しなかったライブラリ(前プロジェクトからの変更点)

[選定要素提案.md](./選定要素提案.md)の各セクションで理由を述べた通り、以下は前プロジェクト(uniteboard)で
使っていたが今回は不採用としたもの。

| ライブラリ | 前プロジェクトでの用途 | 不採用の理由 |
|---|---|---|
| bullmq / @nestjs/bullmq | 通知メール送信のジョブキュー | 通知機能自体を今回のスコープ外としたため不要(選定要素提案.md 5章) |
| ioredis / redis(インフラ) | BullMQのバックエンド | 同上 |
| nodemailer / @nestjs-modules/mailer / handlebars | メール送信・テンプレート | 同上 |
| @dnd-kit/* | カンバンボードのドラッグ&ドロップ | 今回のドメイン(イベント一覧・詳細)にD&D操作が存在しないため不要 |
