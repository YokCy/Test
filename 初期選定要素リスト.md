# 初期選定要素リスト

社内イベント運営プラットフォームの開発にあたり、詳細設計・実装に入る前にチームで議論し、方針を決定すべき選定要素を洗い出したもの。各項目は「何を決めるべきか」を示す問いであり、答えそのものではない。

---

## 1. 全体アーキテクチャと技術スタック

### 選定・議論が必要なこと

- フロントエンドとバックエンドを分離した構成にするか、それとも単一アプリ(SSR/フルスタックフレームワーク)にするか(例: SPA + REST API / Next.js フルスタック / Rails+ERBのようなモノリス)
- バックエンドの言語・フレームワークは何にするか(例: Node.js+NestJS / Node.js+Express / Python+FastAPI / Ruby on Rails / Go)
- フロントエンドの技術は何にするか、そもそも必要か(例: React / Vue / Next.js / サーバーサイドテンプレートのみ)
- データベースは何を使うか(例: PostgreSQL / MySQL / SQLite)
- ORM/クエリビルダを使うか、生SQLで書くか(例: Prisma / TypeORM / Sequelize / Knex)
- モノレポ構成にするか、フロントとバックを別リポジトリにするか
- 状態管理・データフェッチ戦略をどうするか(フロント採用時)(例: React Query / SWR / Redux / Zustand)
- バリデーションライブラリを何にするか、フロント・バックで共有するか(例: Zod / Yup / class-validator)
- リアルタイム性(参加状況のライブ更新など)は必要か、必要ならどう実現するか(例: ポーリング / WebSocket / SSE / 不要としスタティックに割り切る)
- 本課題における「必須」実装のスコープを技術的にどこまでMVPとして固めるか(推奨機能の着手優先度をどう判断するか)

---

## 2. コード管理とディレクトリ構成

### 選定・議論が必要なこと

- リポジトリ構成の基本方針をどうするか(例: `backend/` `frontend/` を並列配置するモノレポ / 完全分離リポジトリ)
- バックエンドのレイヤー構成をどう分けるか(例: Controller-Service-Repository / ドメイン駆動でfeatureごとに分割 / MVC)
- フロントエンドのディレクトリ構成をどう分けるか(例: feature-based / atomic design / pages-based)
- 共通の型定義(User, Event, Category等)をフロント・バックでどう共有するか(例: 共有パッケージ化 / OpenAPIから自動生成 / 個別定義して手動同期)
- Prismaスキーマなど「ドメインモデルの単一の情報源」をどこに置くか(例: backend配下で一元管理し、フロントは型生成のみ利用)
- 命名規則・ファイル命名規則をどう統一するか(例: kebab-case / PascalCase コンポーネント名)
- コミット規約・ブランチ戦略をどうするか(例: Conventional Commits / GitHub Flow / トランクベース)
- Lint/Formatterの共通設定をどう管理するか(例: ESLint+Prettier をルートで一元管理 / パッケージごとに個別設定)
- テストコードの配置場所をどうするか(例: `__tests__` 併置 / `tests/` ディレクトリに集約)
- マニフェスト(未実装事項・設計判断の理由を記載する成果物)をどこに、どの粒度で置くか

---

## 3. セキュリティ・認証・認可

### 選定・議論が必要なこと

- JWTの実装方式をどうするか(例: アクセストークンのみ / アクセス+リフレッシュトークンの2段構成)
- JWTの保存場所をフロント側でどうするか(例: httpOnly Cookie / localStorage / メモリ保持)
- トークンの有効期限をどう設定するか、リフレッシュトークンの失効・再発行フローをどう設計するか
- パスワードハッシュ化のアルゴリズムとコスト値をどうするか(例: bcrypt saltRounds=10 / argon2)
- ロールベースの認可(member/admin)をどの層で強制するか(例: ミドルウェア/Guard層で一元化 / 各エンドポイントで個別チェック)
- 「主催者権限」(イベント作成者本人であること)をどう判定・実装するか(例: リクエストごとにDBでcreatedByを照合するミドルウェア/デコレータを用意する)
- 認可エラー時のレスポンス設計をどうするか(例: 403とその他エラーの使い分け、エラーメッセージの詳細度)
- ログアウト時、既発行トークンをどう無効化するか(例: リフレッシュトークンをDBで管理し失効させる / ステートレスに割り切りクライアント側破棄のみとする)
- CORSやCSRF対策の方針をどうするか(SPA+APIの場合)
- レート制限・ブルートフォース対策を実装するか、しないか
- 入力値のサニタイズ・SQLインジェクション/XSS対策をどの層で担保するか(ORM利用による担保で十分か、追加対策が必要か)
- 監査ログ(誰がいつ何をしたか)を残す範囲をどうするか(admin操作、強制キャンセル等)

---

## 4. データ管理と主要なビジネスルール

### 選定・議論が必要なこと

- カテゴリ削除時、紐づくイベントがある場合の扱いをどうするか(例: 物理削除を拒否しエラーを返す / 論理削除(isActiveフラグ)にする / イベントを既定の別カテゴリへ自動移動する)
- イベントの削除方式を物理削除にするか論理削除にするか(例: `deletedAt` によるソフトデリート / レコード完全削除、一覧・詳細・検索クエリへの影響も含めて検討)
- 主催者は自分のイベントに「参加登録」という行為自体を行うのか、それとも別枠で自動的に参加確定扱いとするか(例: ProjectMemberのような別テーブルで暗黙的に扱う / 参加登録テーブルに主催者用の特別ステータスを持たせる)
- 定員・締切・キャンセル期限のデフォルト値(「開催前まで」)をどう実装するか(例: null許容カラムにしてクエリ側でnow<開催日時をデフォルト条件にフォールバックさせる)
- 定員超過時のキャンセル待ち昇格処理の同時実行制御をどう行うか(例: DBトランザクション+行ロック(SELECT FOR UPDATE)/ 楽観ロック)
- キャンセル待ちの待機順(position)の付け直し方式をどうするか(例: 連番を都度振り直す / ソート用のfloat値を使い挿入時のみ計算/欠番のまま順序はcreatedAtで担保する)
- 繰り上げ履歴(誰が誰から繰り上げられたか)をどのテーブル構造で保持するか
- 出席率計算(出席÷(出席+欠席))において「未マーク」をどう表現するか(例: 出席ステータスをnull許容のenumにする)
- 開催日時に達する前の出席マーク禁止、過去日時のイベント作成禁止など「日時に依存するバリデーション」をどの層(DB制約/アプリケーション層/フロント)でどこまで担保するか
- 編集で開催日時を変更する際の「参加者への警告」をどう実装するか(ログ出力なのか、レスポンスに警告フラグを含めてUI表示するのか)
- フィードバックの1人1件制限・匿名投稿・非公開化をDBスキーマ上どう表現するか(例: `(taskId, userId)` の複合ユニーク制約、`isAnonymous`/`isHidden`フラグ)
- 非公開化されたレビューの集計除外を、集計時のクエリ条件で担保するか、集計値を非正規化して保持するか
- タグの正規化方針をどうするか(例: 完全自由入力のまま保存 / 大文字小文字・空白をトリムして正規化 / 将来的なタグマスタ化を見据えた設計にするか)
- 日時・タイムゾーンの扱いをどう統一するか(例: すべてUTCで保存しフロントでJSTに変換)

---

## 5. 通知システム

### 選定・議論が必要なこと

- 本課題スコープで通知機能をどこまで実装するか(例: 未実装として明記し布石(繰り上げ履歴等)のみ残す / 画面内通知一覧のみ実装するか)
- 通知の配信方式をどうするか(例: アプリ内通知(Notificationテーブル+一覧画面)のみ / メール送信も行う / プッシュ通知は対象外とする)
- どのイベントを通知トリガーとするか(例: 参加登録の担当割当、承認/却下、コメントメンション、締切間近)を今回のスコープでどこまで対応するか
- 通知の既読管理をどう行うか(例: `isRead` フラグの単純な更新 / 既読日時を保持)
- 将来の通知機能拡張(繰り上げ発生時の通知など)を見据えて、今回どこまでイベント発生の記録(履歴テーブル)を残しておくか
- 通知の非同期処理化が必要か(例: 同期的にDB書き込みのみで十分 / キュー/ジョブワーカーを導入するか)

---

## 6. API設計と認可方針

### 選定・議論が必要なこと

- APIのアーキテクチャスタイルをどうするか(例: REST / GraphQL)
- エンドポイント設計の粒度をどうするか(例: リソース単位のRESTfulな設計 / 画面単位のBFF的エンドポイント)
- 一覧系API(検索・カテゴリ絞り込み・タグ検索・ソート)のクエリパラメータ設計をどうするか(例: `?category=&keyword=&tags=&sort=`)
- ページネーション方式をどうするか(例: オフセット方式 / カーソル方式)、必須要件で明示されていないため実装するか判断する
- 参加登録・キャンセル・キャンセル待ち昇格など状態変更系APIのエンドポイント分割方針をどうするか(例: `POST /events/:id/register` のような専用アクションエンドポイント / 汎用PATCHでステータス変更)
- 出席マーク、フィードバック投稿など「特定条件を満たさないと実行不可」な操作のエラーレスポンス設計をどう統一するか(例: バリデーションエラーの共通エラーコード体系)
- レスポンスの型(参加者一覧に匿名投稿者情報を含めるか否かなど、ロールによってレスポンス内容を出し分ける必要がある箇所)をどう設計するか
- APIバージョニングの方針をどうするか(今回のスコープで必要か)
- API仕様書をどう管理するか(例: OpenAPI/Swagger自動生成 / Markdownで手動管理)
- エラーレスポンスのフォーマットを統一するか(例: `{ code, message, details }` の共通形式)

---

## 7. 画面構成とUI/UX

### 選定・議論が必要なこと

- UIコンポーネントライブラリ・デザインシステムを使うか、独自実装にするか(例: MUI / Chakra UI / Tailwind CSS + 自前コンポーネント)
- ルーティング方式をどうするか(例: SPAのクライアントサイドルーティング / ページ単位のサーバーサイドルーティング)
- 「参加登録」ボタンが状態(空席あり/満席/締切済み/キャンセル待ち中/主催者本人)によって表示を出し分けるロジックをフロント・バックのどちらの責務にするか
- マイページの集計情報(累計参加数・出席率・カテゴリ別傾向)をリアルタイム集計するか、事前計算して保持するか
- 出席管理画面(主催者向け)の一括操作(全員出席にする等)を提供するか
- フォームバリデーションのフィードバックをどう表示するか(例: リアルタイムバリデーション / 送信時のみ)
- レスポンシブ対応の要否・対象デバイスをどこまでとするか
- ローディング状態・エラー状態・空状態(イベントが0件等)のUI設計方針を統一するか
- admin向け画面と一般ユーザー向け画面を別アプリとして分離するか、同一アプリ内でロール制御して出し分けるか

---

## 8. インフラと開発環境

### 選定・議論が必要なこと

- ローカル開発環境の構築方法をどうするか(例: Docker Compose でDB+アプリを起動 / ローカルに直接インストール)
- DBマイグレーション管理をどう行うか(例: Prisma Migrate / 手動SQLスクリプト)
- Seedデータ(初期ユーザー・カテゴリマスタ等)の投入方法・内容をどう設計するか
- 環境変数・シークレット管理をどうするか(例: `.env` ファイル + `.gitignore` 徹底、JWT秘密鍵の管理方法)
- テスト環境をどう用意するか(例: テスト専用DB、Dockerでの使い捨てDB)
- CI(自動テスト・Lint)を導入するか、その場合どのタイミングで実行するか
- デプロイ先・デプロイ方式を検討するか(本課題の提出物が「動くコード」までなのか、デプロイ済み環境も含むのか)
- ログ出力の方針をどうするか(例: 構造化ログ、警告ログ出力(開催日時変更時の警告等)の実装場所)
- Node.jsやパッケージマネージャのバージョン固定方針をどうするか(例: `.nvmrc`, `packageManager` フィールド、pnpm/yarn/npmの選定)
