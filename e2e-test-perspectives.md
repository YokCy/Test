# E2Eテスト観点リスト

[画面設計仕様.md](./画面設計仕様.md)3章（画面レイアウト・状態遷移図・3.4節UI/UX設計方針）と
[docs/manifest.md](docs/manifest.md)「4. 画面遷移・構成」を基にした、Playwright E2Eテストの観点リスト。
[test-agent](.claude/agents/test-agent.md)のE2Eガイドライン（`data-testid`セレクタ、自動待機、
画面横断のゴールデンパスに絞る方針）に沿って、ユニットテスト（Jest/Vitest）ではカバーしきれない
「複数画面・複数ユーザーをまたぐ一連の操作」に対象を絞る。個々の画面・コンポーネント単位の分岐は
[backend-unit-test-perspectives.md](./backend-unit-test-perspectives.md)・
[frontend-unit-test-perspectives.md](./frontend-unit-test-perspectives.md)でカバー済みのため対象外。

凡例: 【シナリオ】画面横断の一連の操作、【権限】ロール・所有関係によるアクセス制御、
【状態反映】操作後の表示更新（TanStack Queryの`invalidateQueries`によるキャッシュ再取得）、
【境界】日時・定員等の境界値

---

## 0. 前提条件・準備作業（テストコード生成前に必要な対応）

### 0.1 `data-testid`の付与が不足している

現状、`frontend/src/`には`data-testid`がほぼ存在しない（`Modal`/`Toast`等の基盤コンポーネントの一部のみ）。
test-agentのE2Eガイドライン（「`data-testid`が存在しない箇所は対象コンポーネントへの属性追加をユーザーに
提案する」）に従い、以下の画面・要素には最低限の`data-testid`が必要。E2Eテストコード生成の前段として、
テスト作成者（`test-agent`）から個別に追加提案を行う想定（本リストは「何が要る見込みか」の洗い出しのみ）。

- ログイン画面: メール/パスワード入力欄、送信ボタン（`getByLabelText`で代替可能なため優先度低）
- イベント一覧: 検索・タグ入力欄、ソートセレクト、各イベントカード（`data-testid="event-card-{id}"`のように識別できるもの）
- イベント詳細: 参加登録/キャンセルの操作ボタン（`RegistrationActionButton`内の各状態のボタン）、編集/削除リンク、出席管理へのリンク、フィードバックを書くリンク
- イベント作成/編集フォーム: 各入力項目、保存ボタン、M-08警告モーダルの「続行」「戻る」ボタン
- 出席管理画面: 各参加者行の出席/欠席ボタン、強制キャンセルボタン
- フィードバック投稿画面: 星評価、匿名チェックボックス、投稿/更新ボタン
- カテゴリマスタ管理: 追加/編集/削除ボタン、M-01のカテゴリ名入力欄

### 0.2 認証まわりの設計

- `playwright.config.ts`は現状`chromium`プロジェクト1つに対し`storageState: "tests/.auth/admin.json"`のみを
  紐付けている。しかしゴールデンパスの大半（イベント作成・参加登録等）は**一般memberとしての操作**であり、
  adminのstorageStateのままでは主催者=admin固定になってしまい、「主催者本人 or admin」の権限分岐
  （画面設計仕様.md M-02, M-05, M-06等）を自然にテストできない。
  → `tests/auth.setup.ts`で`admin.json`に加え`member.json`（例: seedのtanaka/sato等）も生成し、
    シナリオに応じて`test.use({ storageState: "tests/.auth/member.json" })`で切り替える2プロジェクト構成
    （またはdescribeブロック単位の`test.use`）に拡張する必要がある。
- 「他ユーザーの操作を横から観測する」シナリオ（例: 満席時の自動繰り上げ、adminによる強制キャンセル・
  フィードバック非公開化）は、1テスト内で複数の認証済みブラウザコンテキストが同時に必要になる
  （`browser.newContext({ storageState: ... })`を用いて、organizer用/participant用/admin用のページを
  同一テスト内で使い分ける）。

### 0.3 テストデータ

- 各テストは自身の`beforeEach`等でイベントを新規作成し、他テストの残存データに依存しない
  （test-agentガイドライン、画面設計仕様.md 3.4節）。
- 定員・締切等の日時条件はテスト内で「現在時刻からの相対値」として計算し、実行タイミングに依存しないようにする
  （例: 開催日時を「今日から7日後」、キャンセル待ち検証用の満席イベントは`capacity: 1`で作成する）。
- 出席マーク・フィードバック投稿のシナリオは「開催日時が過去であること」が前提条件のため、
  UIのイベント作成フォームでは`startAt`に過去日時を指定できない（バリデーションで拒否される、
  画面設計仕様.md 3.1.4「作成時`startAt > now`」）。
  - `EventsService.create`/`update`双方が`assertFutureStartAt`をサーバー側でも検証するため
  （[events.service.ts](backend/src/modules/events/events.service.ts)）、`POST /events`をAPI直接呼び出しに
    差し替えても過去日時での作成はできない（当初検討した「APIを直接叩いて過去日時イベントを作る」方式は
    不可能と判明）。
  - DBへ直接書き込んで`startAt`を過去に書き換える方法もあるが、これは業務ルール（開催前の出席マーク・
    フィードバック投稿禁止）を検証する意味を失わせる本物のバックドアになるため採用しない。
  - 採用する方式: `startAt`を「現在時刻から数十秒後」の短い未来時刻に設定してイベントを作成し、
    実際にその時刻が経過するまでテスト内で待ってから出席マーク・フィードバック投稿の操作へ進む
    （`page.waitForTimeout`で固定時間待機すること自体はUI状態待ちには使わない方針だが、ここでは
    「UIの描画待ち」ではなく「業務上の時刻経過を待つ」ものであり性質が異なる。バッファを含め30秒程度を
    見込む）。実行時間は延びるが、UI操作・サーバーの実際の時刻判定を素通りせず検証できる。
  - 過去に手動確認時（frontend-tasks.md 11.4）も同じ制約（過去日時イベントを用意できない）で
    未実施となっていた経緯があり、この方式で解消できる。

---

## 1. 認証・アクセス制御横断

- 【シナリオ】未ログイン状態で`/events`等の保護されたURLへ直接アクセス→`/login`へリダイレクトされること
- 【シナリオ】ログイン→イベント一覧が表示される→ログアウト→`/login`に戻り、再度保護URLへアクセスするとリダイレクトされること
- 【権限】member権限で`/admin/categories`へ直接アクセス→404画面（P-10）が表示されること（画面設計仕様.md 3.1.9）
- 【権限】adminでログインした場合のみヘッダーに「カテゴリ管理」導線が表示され、そこから遷移できること

---

## 2. ゴールデンパス: イベント作成→参加登録→出席マーク→フィードバック投稿

主要な一連の流れ（画面設計仕様.md 3.4節で明記されたゴールデンパス）。organizer用・participant用の
2ユーザー（member）が必要。

1. 【シナリオ】memberAでログインし、「＋新規作成」からイベントを作成する（タイトル・開催日時・定員2名・カテゴリを入力）→保存後イベント詳細に遷移し、入力内容が表示されること
2. 【シナリオ】同イベントをmemberBで開き、「参加登録する」→`CONFIRMED`として登録され、ボタン表示が「キャンセルする」に切り替わること（画面設計仕様.md 3.2状態遷移）
3. 【権限】memberAの画面には「あなたが主催者です」と表示され、参加登録ボタン自体が出ないこと（`ORGANIZER`状態）
4. 【状態反映】memberBの登録後、イベント一覧・詳細双方の「残り n/capacity名」表示が更新されること（画面再読み込みまたは操作後の再取得で反映。画面設計仕様.md 3.4節「リアルタイム同期は導入しない」の前提通り）
5. 【シナリオ】開催日時経過後（0.3節の方式Aで過去日時イベントとして準備）、memberAが出席管理画面でmemberBを「出席」としてマークする→マーク済み表示（●出席）になること
6. 【シナリオ】memberBがフィードバック投稿画面へ遷移し、星評価・コメントを入力して投稿する→イベント詳細のレビュー一覧に反映されること
7. 【シナリオ】同じmemberBが再度フィードバック投稿画面を開くと、投稿済み内容で初期化され「更新する」ボタンになっていること

---

## 3. 満席時のキャンセル待ち登録→キャンセル→自動繰り上げ

`capacity: 1`のイベントで、memberA=主催者、memberB=1人目(CONFIRMED)、memberC=2人目(WAITLISTED)。

1. 【境界】memberBが登録→`CONFIRMED`になること
2. 【境界】memberCが登録→定員超過のため自動的に`WAITLISTED`として登録され、「キャンセル待ち中（1番目）」と表示されること
3. 【シナリオ】memberBが「キャンセルする」を実行（確認モーダルM-04で「繰り上げが発生する場合があります」の注記を確認した上で確定）
4. 【状態反映】キャンセル後、memberCの画面（再読み込み後）で`registrationState`が`CONFIRMED`に自動繰り上がっていること
5. 【シナリオ】繰り上げ後、イベント詳細の参加者数・空き状況表示が正しく更新されていること

---

## 4. イベント編集（開催日時変更警告モーダルM-08）→削除

1. 【シナリオ】主催者がイベント編集画面を開き、開催日時以外の項目（タイトル等）のみ変更して保存→M-08を経由せずそのまま保存が完了すること
2. 【シナリオ】既に参加者（memberB）が`CONFIRMED`で登録済みのイベントで、主催者が開催日時を変更して保存→
   **`PUT /events/:id`は既にこの時点で成功しており**（`StartAtChangeWarningModal.tsx`のWHYコメント参照。
   本モーダルは保存前の確認ではなく「参加登録者がいる旨を主催者に知らせる事後通知」）、M-08警告モーダルが
   表示されること
3. 【シナリオ】M-08で「続行」を選択→そのままイベント詳細画面へ遷移すること（更新自体は手順2の時点で
   既に確定済み。「続行」は画面遷移のみを行う）
4. 【シナリオ】別ケースでM-08表示後「戻る」を選択→更新は取り消されず（取り消すAPI自体が存在しない）、
   モーダルが閉じて編集画面に留まること。画面を再読み込みすると、変更後の値がサーバーに反映済みで
   あることを確認する（「戻る」＝保存キャンセルだと誤解しないこと自体がこのテストの主目的）
5. 【シナリオ】編集画面から「このイベントを削除」→削除確認モーダルM-02→確定でイベント一覧から当該イベントが消えていること
6. 【権限】主催者でもadminでもないmemberが、URLを直接叩いて他人のイベント編集画面（`/events/:id/edit`）にアクセスした場合の挙動（403相当のエラー表示、または一覧への差し戻し）を確認する

---

## 5. カテゴリマスタ管理（admin専用）

1. 【シナリオ】adminで`/admin/categories`にアクセスし、「＋追加」からカテゴリを新規作成→一覧に反映されること
2. 【シナリオ】同名カテゴリを重複作成しようとする→M-01モーダルを閉じずにフィールドエラー（サーバー`409`）が表示されること
3. 【シナリオ】紐づくイベントが0件のカテゴリを削除→確認モーダルM-03→確定で一覧から消えること
4. 【シナリオ】紐づくイベントが1件以上あるカテゴリを削除しようとする→M-03内にサーバー`409`エラーメッセージがそのまま表示され、一覧からは消えないこと

---

## 6. 権限に応じた操作（admin限定操作）

1. 【権限】出席管理画面で、adminのみに「強制キャンセル」ボタン（M-05）が表示され、memberには表示されないこと
2. 【シナリオ】キャンセル可能期限を過ぎた確定参加者に対し、adminが強制キャンセル（M-05）を実行→対象者が`NOT_REGISTERED`に戻ること（期限を無視して実行できることの確認）
3. 【権限】イベント詳細のレビュー一覧で、adminのみに「非公開化」ボタン（M-06）が表示されること
4. 【シナリオ】adminがフィードバックを非公開化（M-06、取り消し不可の警告文言を確認した上で確定）→一般memberの画面（再読み込み後）でそのレビューが表示されなくなること、平均評価の再計算に反映されること
5. 【シナリオ】匿名投稿されたフィードバックが、一般member視点では投稿者名の代わりに固定文言（「匿名希望」）で表示され、admin視点では投稿者名が表示されること

---

## 7. マイページの反映確認

1. 【シナリオ】あるユーザーが「主催イベント」「参加予定」「参加履歴」の3タブそれぞれで、実際に主催・参加登録・出席済みのイベントが正しく振り分けられて表示されること
2. 【シナリオ】タブ切り替えがURLを変えずに行われ、ブラウザの戻るボタンでタブ切り替え前の画面（マイページに来る前の画面）に戻ること（画面設計仕様.md 3.1.5「タブ切り替えはURLクエリ等に反映しない」の確認）
3. 【シナリオ】出席マーク＋フィードバック投稿後、累計参加数・出席率・カテゴリ別集計に反映されていること

---

## 8. エッジケース・意地悪テスト

凡例に加え、【連打】多重送信・多重クリック、【競合】複数ユーザーの同時操作によるレースコンディション。

1. 【境界】登録締切（`registrationDeadline`、未設定時は`startAt`）を過ぎた`CLOSED`状態のイベントで、
   「登録締切を過ぎました」ボタンをクリックしても`POST /events/:id/register`が呼ばれないこと
   （`disabled`表示だけでなく、実クリックで何も起きないことまで確認する）
2. 【境界】キャンセル可能期限（`cancellationDeadline`）を過ぎた確定参加者本人が、自分でキャンセルボタンを
   実行しようとした場合の挙動（memberの通常キャンセルは期限超過で拒否され、admin強制キャンセルのみ
   可能であることをUI操作で確認する。6章のM-05はadmin視点のみのため、ここではmember視点の拒否を見る）
3. 【連打】参加登録ボタンを短時間に連続クリックしても、二重登録（同一ユーザーに対する`Registration`行の
   重複作成）が起きないこと（送信中`disabled`の実効性確認。2クリック目はボタンが既に無効化されているか、
   サーバー側の一意制約で弾かれるかのいずれかで結果的に1件のみになること）
4. 【連打】フィードバック投稿ボタンを連続クリックしても、二重投稿（1人1件制限）にならないこと
5. 【シナリオ】投稿条件（開催終了済み＋`ATTENDED`）を満たさないイベントのフィードバック投稿URL
   （`/events/:id/feedback`）へ、他画面を経由せず直接アクセスする→フォームの代わりに理由メッセージが
   表示され、実際のサーバー`403`応答を経由して表示されていること（フロント単体テストではモックで
   確認済みだが、実バックエンドを通した経路として改めて確認する）
6. 【競合】`capacity: 1`のイベントに対し、memberB・memberCがほぼ同時に参加登録を実行した場合、
   最終的に片方のみ`CONFIRMED`・もう片方が`WAITLISTED`になり、`CONFIRMED`が2件になることはないこと
   （繰り上げ・定員判定のトランザクション排他制御が実環境でも機能するかの確認。2つの`request`コンテキスト
   から`Promise.all`でほぼ同時に`POST /events/:id/register`を呼び出す形で検証する）
7. 【シナリオ】同一ブラウザで2つのタブを開き、タブ1でユーザーAとしてログイン後、タブ2で別ユーザーBとして
   ログインし直す→タブ1を操作するとユーザーBとしてのセッションになっていること（httpOnly Cookieが
   ブラウザ全体で共有される既知の仕様。過去の手動確認で「別ユーザーのはずが403にならない」という
   誤解を招いた経緯があるため、意図した挙動として回帰確認しておく）
8. 【シナリオ】イベント一覧取得等のAPI呼び出しが失敗する状況（例: Playwrightの`route.abort()`/
   `route.fulfill({status:500})`でAPIレスポンスを強制的にエラー化する）を作り、画面がクラッシュせず
   エラー表示になること

---

## 9. セキュリティテスト（認可バイパス・情報漏えい）

社内ツールとはいえ認可の壁を実際に叩いて確認する、防御的な観点のテスト。ただし**実データを削除・
無効化・書き換えるような、当たれば本体に深刻な被害が及ぶ操作（`DELETE`/`deactivate`/降格等の破壊的
リクエスト）は対象に含めない**。認可漏れがあった場合に備え、「万一失敗しても実害が読み取られる
（情報漏えい）だけで済む」読み取り系IDOR・認証バイパスに絞る。UI操作ではなくPlaywrightの`request`
フィクスチャで直接APIを呼び出す形のテストが中心になる（ブラウザのUIには無い操作を「攻撃者は直接
APIを叩いてくる」前提で再現するため）。書き込みが要るテストも、必ずテスト自身が作成した使い捨て
データのみを対象にする（他人・シード済みアカウントの状態を変更しない）。

### 9.1 未認証・認可バイパス（読み取り系IDOR・情報漏えい）

1. 【攻撃】Cookie無し（未認証）状態で、あらゆる保護対象API（例: `GET /events`, `GET /events/:id`,
   `GET /users/me/stats`）を直接呼び出す→`401`になり、レスポンスbodyにデータが一切含まれないこと
2. 【攻撃】無効・改ざんした値や期限切れのJWT（Access Token）をCookieに設定した状態で保護APIを呼び出す
   →`401`になり、他人の情報が閲覧できないこと
3. 【攻撃】memberAのログイン済みCookieで、memberBが主催するイベントの参加者一覧API
   （`GET /events/:id/registrations`、主催者本人/admin限定）を直接呼び出す→`403`になり、
   参加者の氏名・出席状況等が読み取れないこと（読み取り専用のIDOR確認。UI上は「出席管理へ」導線が
   非表示なだけで、APIレベルでも守られていることを確認する）
4. 【攻撃】member権限のCookieで、admin専用の一覧・詳細取得API（例: `GET /users`）を直接呼び出す→
   `403`になり、全ユーザーの一覧（emailやロール等）が読み取れないこと
5. 【攻撃】非公開化（`isHidden`）されたフィードバックが、一般member視点で`GET /events/:id/feedbacks`の
   レスポンスbody自体に一切含まれていないこと（UI表示上隠しているだけでなく、開発者ツールの
   ネットワークタブ等からも見えない設計になっているかを、レスポンスJSONを直接検査して確認する）
6. 【攻撃】匿名投稿されたフィードバックについて、一般member視点の`GET /events/:id/feedbacks`レスポンス
   で`author`が確実に`null`になっており、他のフィールド（`userId`等の内部識別子）からも投稿者が
   推測できる形で漏れていないことをレスポンスJSONで確認する
7. 【攻撃】ログイン試行で、存在しないemail・パスワード不一致・無効化済みアカウントの3パターンを試し、
   エラーメッセージがすべて同一であることを確認する（ユーザー列挙攻撃対策。実データの変更を伴わない
   読み取り専用の確認。バックエンドユニットテストで検証済みの内容をE2Eでも実際のレスポンス文言として
   再確認する）

### 9.2 入力値攻撃（自分が作成したテストデータの範囲内のみ）

8. 【攻撃】自分（テスト実行ユーザー）が作成したイベントのタイトル・説明文・自分のフィードバック
   コメントに`<script>alert(1)</script>`等のHTML/JSを含む文字列を入力して投稿する→表示時に
   スクリプトが実行されず、エスケープされたテキストとして表示されること（Reactのデフォルト
   エスケープに頼っている箇所が`dangerouslySetInnerHTML`等で破られていないことの確認。対象は
   常に自分が作成したテスト用データのみとし、他ユーザーのデータには一切書き込まない）
9. 【攻撃】自分が作成するイベントの`POST /events`bodyに、スキーマに無いフィールド
   （例: `organizerId`を自分以外の値で指定、`role: "ADMIN"`等）を追加して送信する→無視される、
   またはZodスキーマにより`400`になり、意図しない値が反映されないこと（マスアサインメント対策。
   作成対象は自分のテスト用イベントのみ）
10. 【攻撃】自分が作成するイベントの`capacity`に負数・0・巨大な数値、自分のフィードバックの`rating`に
    範囲外の値（0や6）をAPI直叩きで送信する→クライアント側バリデーションを経由しなくてもサーバー側
    `400`で拒否されること（フロントの入力制約に依存しない、サーバー側バリデーションの実効性確認。
    いずれも自分のテスト用データに対してのみ行う）
