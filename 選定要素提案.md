# 選定要素提案

[初期選定要素リスト.md](./初期選定要素リスト.md)で洗い出した論点について、**ユーザビリティ**と**実装の複雑さ**の
バランスを基準に提案する。前提: リポジトリは単一(モノレポ)、コーディング規約は別途決定。

各項目は「提案」であり最終決定ではない。異論があれば都度上書きしてほしい。
各表に「検討したが不採用の案」列を追加し、比較検討の過程を残す。

## 絶対要件チェック(課題の必須技術スタック)

課題で指定された必須技術スタック「TypeScript / React / Express / Prisma / PostgreSQL」に対する充足状況。

| 要件 | 状態 | 備考 |
|---|---|---|
| TypeScript | ✅ 満たす | backend/frontend共通 |
| React | ✅ 満たす | frontend |
| Express | ✅ 満たす(ユーザー確認済み、2026-09-01) | backendはNestJSを採用。NestJSは内部で`@nestjs/platform-express`([backend/package.json](./backend/package.json))経由でExpressをHTTPサーバーとして使用しており、これで満たすと判断した。生のExpressコード(`app.get()`等)は書かない |
| Prisma | ✅ 満たす | backend/prisma |
| PostgreSQL | ✅ 満たす | docker-compose `db`サービス |

---

## 1. 全体アーキテクチャと技術スタック

前プロジェクト(uniteboard)と同じスタックを継続する(既にscaffold済み)。理由は主に実装複雑度: 既に
認証基盤・共通UI・テスト環境が動く状態であり、変更する積極的な理由が要件から見当たらないため。

| 論点 | 提案 | 理由 | 検討したが不採用の案(理由) |
|---|---|---|---|
| 構成方式 | SPA(React) + REST API分離 | モーダル遷移・状態出し分けが多い画面はSPAが素直。継続で複雑度ゼロ | Next.js等によるBFF/SSR構成 — 認証・APIコール経路が増え、社内小規模ツールには過剰。既存のSPA+REST資産も再利用できなくなる |
| BE | NestJS + Prisma + PostgreSQL | ドメインが増える(Event/Category/Registration/Waitlist/Attendance/Feedback)構成でもモジュール分割しやすい | Express+素のクエリビルダ(Knex等) — NestJSのDI/Guard/Moduleの恩恵をゼロから再構築するのは非合理。Go/Rails等への言語切り替え — 学習コスト増、Zodによる型共有資産も失う |
| FE | React + Vite + TypeScript + Tailwind | 継続、UI資産(Button/Modal/Toast)を流用できる | Vue/Svelte — 既存コンポーネント資産がReact前提のため移植コストが発生するだけでメリットがない |
| 状態管理 | TanStack Query | サーバー状態のキャッシュ管理に十分。Redux等は過剰 | Redux Toolkit — クライアント状態管理に強みがある反面、サーバー状態のキャッシュが主目的の今回は定型コードが増えるだけ |
| リアルタイム性 | **不要**。操作後にTanStack Queryのキャッシュ無効化で最新化 | 要件は「通知はログ/ステータス表示で可」なので即時反映の必要性が薄い。WebSocket導入は複雑さに見合わない | WebSocket(Socket.io等) — 常時接続管理・再接続処理・スケーリングのコストが、「定員の即時反映」程度の要件には見合わない |

---

## 2. コード管理とディレクトリ構成(確定事項の補足のみ)

- リポジトリ: 単一(モノレポ、pnpm workspace + turbo) — 確定
- ディレクトリ構成: 前プロジェクト踏襲(`backend/src/{common,modules,prisma}`、`frontend/src/{features,components,router,lib}`)
- コーディング規約: [docs/coding_standards.md](docs/coding_standards.md)に確定(MANIFEST.mdの技術スタック・ディレクトリ構成に沿って全面改訂済み)

---

## 3. セキュリティ・認証・認可

| 論点 | 提案 | 理由 | 検討したが不採用の案(理由) |
|---|---|---|---|
| トークン方式 | Access+Refresh(2段)、httpOnly Cookie | 継続実装済み。ログイン維持(UX)とAccessToken短命化(セキュリティ)を両立でき、追加コストなし | サーバー側セッションストア(セッションCookie) — Redis等の別ストアが必要になり、通知基盤(BullMQ/Redis)を見送った判断と矛盾する |
| 主催者権限判定 | CASLでEvent subjectに`createdBy`条件を追加 | Project/Taskで実績あるパターンをそのまま再利用でき複雑度が低い | 独立の`Organizer`ロールテーブル新設 — 主催の移譲・降格等のライフサイクル管理が要件にないのに状態が増えるだけ |
| 認可エラー | 403 + 日本語の分かりやすいメッセージ | 社内ツールなので詳細度優先でUX向上、セキュリティ上のリスクは小さい | 汎用的な「権限がありません」に統一(内部情報の秘匿を優先) — 社内限定システムで内部攻撃者を想定した秘匿よりユーザビリティを優先する判断 |
| CORS/CSRF | Origin限定 + SameSite=Lax | 社内利用限定のためCSRFトークンまでは過剰。Laxで実用上十分 | Double Submit Cookie等のCSRFトークン追加実装 — SameSite=Laxで大半のクロスサイトリクエストを防げる上、外部埋め込み等の想定もなく複雑度に見合わない |
| レート制限 | ログイン試行回数制限のみ実装、それ以外は見送り | 実装コストが低い割にブルートフォース対策の効果が大きい箇所だけ対応 | 全API共通のレート制限(nestjs-throttler等) — 認証済み社員のみが叩く前提のAPIに一律制限をかけると、誤操作でロックされるリスクの方が実害として大きい |
| 監査ログ | admin操作(強制キャンセル・レビュー非公開化等)のみアプリログに出力 | 要件が求めるのは「警告ログ出力」レベルで十分 | 専用`AuditLog`テーブルで全操作を永続化 — 要件を超える過剰実装。将来監査要件が具体化すれば再検討 |
| JWTペイロードのrole | 含めない。`sub`(userId)のみとし毎リクエストDBから最新roleを引き直す | 昇格/降格・無効化がAccess Token残り有効期限(最大15分)に関わらず即座に反映される | roleをペイロードに含める(要件文言通り) — ユーザーに確認したところ「特にこだわりなし」との回答のため、より安全な現行方式(DB毎回引き直し)を推奨案として採用した |
| Refresh Tokenの使い回し検知 | 追加する。失効済み(ローテーション済み)トークンの再送信を検知したら、該当ユーザーの全Refresh Tokenを一括失効させる | トークン盗難時の被害を最小化できる。実装コストは小さい(`rotate()`内のrevokedAt済み検知時に全件revokeするだけ) | 追加しない(現状維持) — ユーザーに確認したところ「特にこだわりなし」との回答のため、被害範囲を抑えられる追加実装を推奨案として採用した(コード自体は未実装、着手時に対応) |

### XSS・SQLインジェクション・パストラバーサル対策

要件上、イベント説明文・フィードバックコメント・タグ名など「ユーザーが入力し他ユーザーに表示される」
データが複数存在するため、明示的に方針を決めておく。

| 脅威 | 提案 | 理由 | 検討したが不採用の案(理由) |
|---|---|---|---|
| XSS(蓄積型) | **Reactの自動エスケープに全面的に依拠**する。`dangerouslySetInnerHTML`は使用しない。イベント説明文・コメント・タグ名は常にプレーンテキストとしてJSXの子要素で描画する。バックエンドはZodで型・文字数のみ検証し、HTMLタグの除去(サニタイズ)は行わない | Reactはデフォルトで子要素・属性値をエスケープするため、`dangerouslySetInnerHTML`を使わない限りJSX描画経路でのスクリプト注入は原理的に発生しない。二重にサニタイズすると実装・テストの手間が増えるだけで得るものが薄い | `sanitize-html`等によるHTMLサニタイズをAPI側に追加 — リッチテキスト/Markdown入力を許可する予定がなく(全項目プレーンテキスト)、Reactの自動エスケープと機能が重複するだけの冗長な対策になる |
| XSS(将来のMarkdown/リッチテキスト対応時) | 今回のスコープでは非対応。将来コメント欄等にMarkdown表示を追加する場合は、その時点で`dangerouslySetInnerHTML`前段に`sanitize-html`/`DOMPurify`等の導入を必須とする | プレーンテキストのままなら不要な依存を増やしたくない。ただし仕様変更時に見落とされやすいので明記しておく | — |
| SQLインジェクション | Prismaのパラメータ化クエリに全面依拠。`$queryRawUnsafe`等の生SQL構築は使用しない | Prismaは標準のクエリメソッド(`findMany`/`create`等)を使う限り自動的にプレースホルダ化されるため、意識せず対策済みになる | 生SQL＋手動エスケープ — 検索(キーワード・タグ)や集計程度であればPrismaの標準API・`$queryRaw`(タグ付きテンプレート、パラメータ化される版)で表現可能なため、生SQL化する理由がない |
| パストラバーサル | 今回の要件に「ファイルアップロード・ユーザー指定パスでの静的ファイル配信」機能が存在しないため、実質的にリスクなし。対応不要 | イベント画像添付等は要件に含まれていない(6章画面一覧にも項目なし)ため、ユーザー入力をファイルパスに使う経路が現状存在しない | 将来イベント画像アップロードを追加する場合に備えたアップロード基盤の先行実装 — 現時点で不要な機能を先取りする複雑度増加であり、要件確定時に別途設計する方が妥当 |
| HTTPセキュリティヘッダー | `helmet`導入を推奨(`X-Content-Type-Options`, `X-Frame-Options`等の既定値適用) | NestJSに`helmet`を1行追加するだけで低コストに基本的なヘッダー対策ができる | 本格的なContent-Security-Policyの設計 — Vite開発サーバーのインラインスタイル注入等との相性確認コストが高く、社内ツールでの優先度が低いため今回は見送り |

---

## 4. データ管理と主要なビジネスルール

**推奨(Should)機能のスコープ確認**: 要件定義の推奨機能のうち、**キャンセル待ち＋自動繰り上げ**と
**フィードバック(星評価＋コメント)は実装対象に含める**(以下の表の各行は、いずれも「実装する」前提での
業務ルール設計)。**通知システムのみ実装対象から除外する**(5章参照)。

| 論点 | 提案 | 理由 | 検討したが不採用の案(理由) |
|---|---|---|---|
| カテゴリ削除 | 紐づくイベントがあれば**物理削除を拒否**(409エラー)。未使用時のみ物理削除可 | 移動・論理削除ロジックが不要になり実装が最も単純。ユーザーにとっても「イベントが消える」事故が起きない | 論理削除(`isActive`フラグで一覧から除外) — 「削除したのに裏に残っている」状態の説明UIが増え複雑化。既定の別カテゴリへ自動移動 — 移動先選定ロジック・確認UIが増える |
| イベント削除 | **論理削除**(`deletedAt`) | 参加履歴・出席率・フィードバックの参照整合性を保ったまま削除表現できる。物理削除だと集計値が壊れる | 物理削除 — 過去の参加履歴・フィードバックの外部キーが失われ、マイページの集計(累計参加数等)が壊れる |
| 主催者の参加登録 | **暗黙的に参加確定扱い**とし、Registrationレコードは作らない。UI側で「主催者」として表示 | 別レコードを作ると「主催者としての登録」と「一般参加登録」の二重登録防止条件が増え複雑になる。マイページの参加履歴には主催イベントも合算して表示すればよい | Registrationに`ORGANIZER`という特別ステータスを追加し明示的にレコードを作る — 二重登録防止の一意制約や各種集計クエリすべてに「主催者かどうか」の分岐が必要になり複雑化する |
| キャンセル待ちの順序 | 新規の待機登録は常に列の末尾(現在の最大`position` + 1)に追加するFIFO方式 | 待機解除が途中で発生しても残りの待機者のposition値の相対順序は変わらないため、全件再採番が不要 | **(訂正)** 当初「挿入時に前後の中間値を採番」と提案していたが、これはKanbanタスクの並べ替え(前プロジェクト由来の発想)向けで、単純なFIFO待機列には過剰。全件再採番方式(離脱・繰り上げの都度`ORDER BY`で振り直し) — 待機者が多いと更新件数・ロック範囲が増える |
| 繰り上げの排他制御 | イベント行を`SELECT ... FOR UPDATE`でロックし、キャンセル削除→先頭繰り上げ→履歴insertを1トランザクションで実行 | 同一イベントへの同時キャンセルは頻度が低く、行ロックの方が実装・検証ともに単純 | 楽観ロック(バージョンカラム+リトライ) — 発生頻度が低い処理にリトライ機構を追加するのは複雑度に見合わない |
| 出席率の未マーク | `AttendanceStatus`をnull許容(ATTENDED/ABSENT/未設定)にし、集計時はnullを除外 | 要件通りで実装もシンプル | 出席率専用の集計テーブルを都度更新 — 書き込みの都度集計更新処理が必要になり複雑。社内規模なら都度計算で十分 |
| 日時バリデーション | アプリケーション層(Service)で判定。DB CHECK制約は導入しない | DB制約は複雑度が上がる割に得るものが少ない(アプリ層で守れば十分) | DB CHECK制約(`start_at > now()`等) — 「現在時刻」比較をSQL側に持たせるとアプリ層のテスト(時刻モック)がしづらくなる |
| 開催日時変更時の警告 | レスポンスに`hasRegisteredParticipants: boolean`を含め、フロントでダイアログ表示 | ログ出力だけよりユーザビリティが高く、実装コストはほぼ同じ | メール/アプリ内通知の送信 — 通知基盤自体を見送っているため対象外 |
| フィードバック制約 | `@@unique([eventId, userId])` + `isAnonymous`/`isHidden`フラグ | 要件通りで実装もシンプル | — |
| タグの正規化 | 完全自由入力のまま保存し、登録時にtrim+小文字化する程度に留める | タグマスタ化は複雑度に見合わない | タグエイリアス機構(表記揺れを正規名にマッピングする別テーブル) — 理想的だが要件にない機能で過剰実装 |
| タイムゾーン | DBはUTC(`@db.Timestamptz`)、表示はdayjsでJST変換 | 前プロジェクトで実装済みのパターンをそのまま再利用 | DBにJSTのまま保存 — サーバー/DBの実行環境のタイムゾーン設定に依存してバグりやすい |

---

## 5. 通知システム

**今回のスコープでは実装しない**ことを提案する。

- 要件でも「通知送信までは不要(ログ出力/ステータス表示で可)」と明記されている
- BullMQ/Redis/メール送信基盤を導入すると、インフラ・依存関係・テストの複雑度が大きく増す一方、必須要件への寄与がない
- ただし「繰り上げ履歴テーブル」だけは保持し、将来の通知機能追加に備える(要件通り)

検討したが不採用: BullMQ+Redis+Nodemailer(前プロジェクトと同構成)の先行導入 — 通知トリガーとなるイベント（繰り上げ発生等）は`PromotionHistory`保存のみで足り、実際にメール/アプリ内通知を配信する機能自体が要件外のため、インフラを増やす理由がない。

---

## 6. API設計と認可方針

| 論点 | 提案 | 理由 | 検討したが不採用の案(理由) |
|---|---|---|---|
| スタイル | REST継続 | 既存の認証API・共通レスポンス形式(`{ success, data }` / `{ success, error }`)をそのまま拡張できる | GraphQL — 学習コスト・スキーマ管理コストが増える割に、今回の画面数・エンドポイント数では恩恵が薄い |
| リソース設計 | `/events`, `/events/:id/registrations`, `/events/:id/feedbacks`, `/categories` | イベント配下にネストする素直なREST設計 | — |
| 状態遷移操作 | 専用アクションエンドポイント(`POST /events/:id/register`, `/cancel`)。満席時のキャンセル待ちも`register`が自動判定 | 汎用PATCHにすると「定員チェック」「締切チェック」等の業務ルールがどのフィールド更新に紐づくか曖昧になる | register/waitlist/cancelを3エンドポイントに分割 — フロントが状態ごとに呼び分ける必要が生じ、ボタンの状態管理ロジックが複雑になる |
| ページネーション | オフセット方式(または省略) | 社内イベントの規模ではカーソル方式は過剰 | カーソルベースページネーションを最初から実装 — 想定データ量(数百件程度)に対してオーバーエンジニアリング |
| エラー形式 | `{ success: false, error: { code, message } }`継続 | 既存の`HttpExceptionFilter`/`ResponseInterceptor`をそのまま流用できる | RFC7807 Problem Details — 既存実装からの移行コストが発生し、社内利用のみでは恩恵が薄い |
| 認可 | `JwtAuthGuard` + `PoliciesGuard`(CASL)継続 | 実装済みの基盤をそのまま拡張 | — |

---

## 7. 画面構成とUI/UX

| 論点 | 提案 | 理由 | 検討したが不採用の案(理由) |
|---|---|---|---|
| UIライブラリ | Tailwind CSS + 自前コンポーネント継続 | 既存資産(Button/Modal/Toast/ConfirmDialog等)を流用でき追加コストがほぼゼロ | MUI/Chakra UI等の導入 — 既存資産を捨てて学習・移行コストを払うメリットがない |
| 参加登録ボタンの出し分け | **バックエンドが計算した`registrationState`(OPEN/FULL/CLOSED/WAITLISTED/REGISTERED/ORGANIZER等)をレスポンスに含め、フロントは表示に徹する** | 定員・締切・自分の登録状態を組み合わせた判定をフロントで再実装すると画面ごとにロジックが重複しバグりやすい | フロントで都度計算(一覧カード/詳細/マイページそれぞれで判定ロジックを実装) — ロジックが複数箇所に散らばりズレるリスクが高い |
| マイページ集計 | リアルタイム集計(都度クエリ)、事前計算(非正規化)はしない | 社内規模ならパフォーマンス上問題なく、書き込み時・読み込み時の二重管理による不整合を避けられる | 集計値をUserテーブル等に非正規化してキャッシュ — 出席マーク訂正時の再計算漏れ等、不整合のリスクが増える |
| 出席管理の一括操作 | MVPでは1人ずつのマークのみ、一括操作は見送り | 優先度が低く実装コストの割に効果が薄い | 「全員出席」一括ボタン — 誤操作で全員を一括出席にしてしまう事故のリスクがある |
| レスポンシブ | PCメイン、Tailwindのbreakpointで最低限の崩れ防止のみ | 社内ツールなのでモバイル最適化への投資は不要 | モバイル専用レイアウトの作り込み — 利用シーン上優先度が低い |
| admin画面 | 別アプリ化せず、同一アプリ内でロール制御(既存`AdminRoute`継続) | 別アプリ化は認証状態共有・デプロイが複雑になるだけでメリットが薄い | adminサブドメイン/別SPA — Cookie共有設定・デプロイ物が2つになる運用コストが増える |

---

## 8. インフラと開発環境

| 論点 | 提案 | 理由 | 検討したが不採用の案(理由) |
|---|---|---|---|
| ローカル環境 | Docker Compose(db/backend/frontendの3サービス)継続 | 既存構成をそのまま利用 | — |
| マイグレーション | Prisma Migrate継続 | 既存ワークフローを利用 | — |
| CI | 導入しない(優先度低として保留) | 提出はGitHubへのアップロードのみで、デプロイ・ホスティングは不要と確定したため | GitHub Actionsでlint/testを自動化 — 完全に無意味ではないが、提出要件に含まれないため今回は見送り。余力があれば追加を検討 |
| 提出形態 | `docker compose up` で一式(db/backend/frontend)が起動すればOK | ユーザー確定事項。デプロイ済み環境の提出は不要 | — |
| ロギング | NestJSの標準Logger、警告ログは該当Service内で出力 | 追加の構造化ログ基盤導入は複雑度に見合わない | Sentry/Datadog等の外部ログ基盤導入 — 社内提出物のスコープを超える |

---

## 確認事項(解消済み)

- **課題の提出形態**: GitHubへのアップロード。デプロイ・ホスティングは不要で、`docker compose up`一発で動作すればOK(ユーザー確定、2026-09-01)。これによりCI/CD・ホスティング先選定は不要と確定した。
