# フロントエンドユニットテスト観点リスト

[docs/manifest.md](docs/manifest.md)「4. 画面遷移・構成」「6. API設計」、[画面設計仕様.md](./画面設計仕様.md)（画面・モーダル仕様、
3.2/3.3節の状態遷移図、3.4節UI/UX設計方針）を基に、実装コード（`frontend/src/features/*`,
`frontend/src/components/*`, `frontend/src/router/*`）と突き合わせて抽出したVitest + React Testing Library
ユニットテスト観点。`test-agent`がテストコードを書く際の一次チェックリストとして使う。

凡例: 【表示】props/APIレスポンスに応じた表示分岐、【操作】ユーザー操作→API呼び出し・コールバック、
【入力】フォームのクライアントサイドバリデーション（React Hook Form + Zod）、
【エラー】サーバーエラー（400/401/403/404/409）のフィールド/画面への反映、
【状態】ローディング・エラー・空データの表示、【遷移】画面遷移・モーダル開閉、
【権限】ロールに応じた表示出し分け（実際のアクセス制御はサーバー側、フロントは表示のみ）

方針（[test-agent](.claude/agents/test-agent.md)のガイドラインを踏襲）:
- 内部state・private関数ではなく、ユーザー操作起点（クリック・入力→表示される結果・呼ばれたAPI）を検証する。
- `apiClient`（`lib/api-client.ts`）はモジュールモックし、実HTTP通信は行わない。
- 日時が絡む判定（開催日時前後、締切前後）は`vi.useFakeTimers()`で現在時刻を固定してから検証する。
- 画面横断のゴールデンパス（例: 参加登録→出席マーク→フィードバック投稿の一連の流れ）はE2E（Playwright）側の
  責務とし、本リストはコンポーネント単体の分岐網羅に絞る。

---

## 1. 共通UIコンポーネント（`components/ui/*`）

### Button
- 【表示】`isLoading`時にスピナー表示＋`disabled`になること
- 【表示】`disabled`props指定時にクリックしても`onClick`が呼ばれないこと

### ConfirmDialog（M-07、M-02〜M-06の基盤）
- 【表示】`isOpen=false`の場合、何も描画されないこと
- 【操作】確定ボタンクリックで`onConfirm`が呼ばれること
- 【操作】キャンセルボタン・（存在すれば）背景クリックで`onCancel`が呼ばれること
- 【表示】`isConfirming=true`の間、確定ボタンが`disabled`＋ローディング表示になること（多重送信防止）
- 【表示】`isDanger`指定時に警告用のスタイル・文言（危険操作であることが視覚的にわかる要素）が適用されること

### Toast
- 【表示】`showToast(message, "error")`呼び出しでエラー用スタイルのトーストが表示されること
- 【状態】一定時間後（またはユーザー操作で）トーストが非表示になること

### Modal
- 【表示】`isOpen=false`の場合、内容が描画されないこと
- 【操作】閉じる操作（×ボタン等）で`onClose`が呼ばれること

---

## 2. router（`router/*`）

### ProtectedRoute
- 【権限】未ログイン（`useMe`が`null`/未認証エラー）の場合、`/login`へリダイレクトすること
- 【権限】ログイン済みの場合、子要素（`Outlet`）が描画されること
- 【状態】`useMe`のロード中はリダイレクトも子要素描画もせず、ローディング表示になること

### AdminRoute
- 【権限】`role !== "ADMIN"`の場合、404/エラー画面（P-10）へ遷移する、またはアクセス不可を示す表示になること
- 【権限】`role === "ADMIN"`の場合、子要素が描画されること

### NotFoundPage（P-10）
- 【表示】404文言と「トップへ戻る」導線が表示されること
- 【遷移】「トップへ戻る」クリックでイベント一覧（`/events`）へ遷移すること

---

## 3. layout（`components/layout/*`）

### Header
- 【権限】`user.role === "MEMBER"`の場合、「カテゴリ管理」リンクが表示されないこと
- 【権限】`user.role === "ADMIN"`の場合、「カテゴリ管理」リンクが表示されること
- 【表示】`useMe`が`undefined`（未取得）の場合、何も描画されないこと（ヘッダーがクラッシュしない）
- 【操作】ユーザーメニューのトグルボタンクリックでメニューの開閉が切り替わること
- 【操作】「ログアウト」クリックでログアウトAPIが呼ばれ、成功後`/login`へ`replace`遷移すること

---

## 4. auth（`features/auth/*`、`LoginPage`/`ProfilePage`は実装済み・他は本リストの対象外）

`LoginPage.test.tsx`・`ProfilePage.test.tsx`で以下は実装済み。新規追加時の重複を避けるため観点のみ記載。

- 【入力】メール/パスワード未入力→バリデーションエラー表示、API未呼び出し（実装済み）
- 【操作】ログイン成功→プロフィール/イベント一覧へ遷移、APIへの送信内容確認（実装済み）
- 【エラー】401エラー→フォーム全体エラーとしてサーバーメッセージを表示（実装済み）
- 【入力】プロフィール表示名の空文字バリデーション（実装済み想定、`ProfilePage.test.tsx`参照）

---

## 5. events（`features/events/*`、P-02イベント一覧・P-03イベント詳細）

### EventsListPage（P-02）
- 【表示】カード上の空き状況表示が`confirmedCount`/`capacity`から「残り n/capacity名」を導出すること（フロントで独自の定員判定をしないこと。画面設計仕様.md 3.1.2）
- 【表示】満席（`confirmedCount >= capacity`）の場合「満席(キャンセル待ち)」表示になること
- 【操作】キーワード検索・タグ絞り込み・開催日順ソートの入力値が`GET /events`のクエリパラメータ（`keyword`/`tags`/`sort`）にそのまま渡ること（実装確認の結果、クライアントサイドの絞り込みではなくサーバーへの問い合わせで実現されている。画面設計仕様.md 3.4「サーバーページネーション無し」は一覧取得自体の話であり、絞り込み条件はサーバーに委譲する実装になっている点をリスト訂正）
- 【遷移】カードクリックで該当イベント詳細（P-03）へ遷移すること
- 【状態】0件の場合の空表示、取得エラー時のエラー表示
- 備考: カテゴリ絞り込みUI・「＋新規作成」導線は本リスト作成時点で未実装（frontend-tasks.md記載の既知の残タスク）。実装後にテスト観点を追加すること。

### EventDetailPage（P-03）
- 【表示】`registrationState`の5値（`NOT_REGISTERED`/`CONFIRMED`/`WAITLISTED`/`ORGANIZER`/`CLOSED`）それぞれで`RegistrationActionButton`に正しい値が渡ること
- 【表示】編集・削除ボタンは主催者本人またはadminのときのみ表示されること（`me.id === organizer.id || me.role === "ADMIN"`）
- 【表示】出席管理へのリンクは主催者本人またはadminのときのみ表示されること
- 【表示】参加者一覧は`CONFIRMED`のみ表示し、`WAITLISTED`は件数バッジのみで個々の氏名を表示しないこと（画面設計仕様.md 3.1.3）
- 【表示】レビュー一覧: 匿名投稿は一般ユーザーには「匿名希望」等の固定文言、adminには投稿者名が表示されること（`author`フィールドのサーバー出し分けをそのまま反映するだけで、フロント側で追加判定をしないこと）
- 【表示】「非公開化」ボタンはadminのときのみ表示されること
- 【操作】「削除」クリックで削除確認モーダル（M-02）が開き、確定で`DELETE /events/:id`が呼ばれ一覧へ遷移すること
- 【状態】取得中のローディング表示、取得失敗時のエラー表示

### RegistrationActionButton（events/detail・my-page 共通コンポーネント）
- 【表示】`registrationState === "ORGANIZER"`→「あなたが主催者です」の非活性テキストのみ
- 【表示】`registrationState === "CLOSED"`→「登録締切を過ぎました」の`disabled`ボタン
- 【表示】`registrationState === "NOT_REGISTERED"`→「参加登録する」ボタン
- 【表示】`registrationState === "WAITLISTED"`かつ`position`指定あり→「キャンセル待ち中（n番目）」表示
- 【表示】`registrationState === "WAITLISTED"`かつ`position`未指定（`null`/`undefined`）→順位無しの「キャンセル待ち中」表示
- 【表示】`registrationState === "CONFIRMED"`→「キャンセルする」ボタン（順位表示は出ない）
- 【操作】「参加登録する」クリック→`register`ミューテーションが呼ばれ、成功で表示が更新されること
- 【操作】register失敗時（`ApiError`）→トースト表示、例外を握りつぶし再スローしないこと
- 【操作】「キャンセルする」/「キャンセル待ちをやめる」クリック→確認モーダル（M-04）が開くこと（即APIを呼ばないこと）
- 【表示】M-04のメッセージが`CONFIRMED`時と`WAITLISTED`時で文言が異なること（繰り上げ注記の有無）
- 【操作】M-04確定→`cancel`ミューテーションが呼ばれ、成功でモーダルが閉じること
- 【操作】cancel失敗時（`ApiError`）→トースト表示、モーダルは開いたままになること（`isConfirmOpen`を閉じる処理がエラー時に実行されないこと）
- 【状態】register/cancel実行中、ボタンが`isLoading`/`isConfirming`表示になること（多重送信防止）

---

## 6. events-form（`features/events-form/*`、P-04イベント作成・P-05イベント編集）

### EventForm（P-04/P-05共通フォーム）
- 【入力】必須項目（タイトル・開催日時・定員・カテゴリ）未入力→バリデーションエラー表示、送信されないこと
- 【入力】定員が1未満・数値以外→バリデーションエラー
- 【入力】終了日時・登録締切・キャンセル期限が未入力の場合、送信データに`undefined`が渡ること（空文字`""`ではないこと。過去に空文字送信でZodの`datetime().optional()`が`400`になった不具合の再発防止、CLAUDE.md/画面設計仕様.md 3.1.4）
- 【入力】タグ追加（`TagInput`）: Enter/カンマ区切り等での追加、重複タグの排除、削除ボタンでの除去
- 【操作】新規作成（P-04）: 送信で`POST /events`が呼ばれ、成功でイベント詳細へ遷移すること
- 【操作】編集（P-05）: 初期値がイベント情報で埋まること（`toFormDefaults`相当のマッピング、日時未設定項目は空欄になること）
- 【操作】編集保存で`hasRegisteredParticipants: true`が返った場合、開催日時変更警告モーダル（M-08）が自動表示されること
- 【操作】M-08で「続行」→編集確定としてイベント詳細へ遷移すること
- 【操作】M-08で「戻る」→フォーム入力内容を保持したままモーダルのみ閉じること（再送信していないこと）
- 【操作】`hasRegisteredParticipants`が`false`（または未含有）の場合、M-08を経由せずそのまま確定・遷移すること
- 【エラー】サーバー側バリデーションエラー（`400`）発生時、フォームを閉じずにフィールド単位でエラーメッセージを表示すること
- 【操作】（P-05のみ）「このイベントを削除」クリック→削除確認モーダル（M-02）を開き、確定で`DELETE /events/:id`が呼ばれ一覧へ遷移すること
- 【状態】送信中、保存ボタンが`disabled`＋ローディング表示になること

---

## 7. my-page（`features/my-page/*`、P-06マイページ）

### MyPage / MyPageTabs
- 【操作】タブ切り替え（主催イベント/参加予定/参加履歴）がコンポーネントのローカル状態のみで行われ、URLクエリ等に反映されないこと（画面設計仕様.md 3.1.5）
- 【表示】各タブに対応するデータ（`organizing`/`upcoming`/`history`）が正しく描画されること
- 【状態】各タブでデータ0件の場合の空表示

### StatsSummary
- 【表示】累計参加数・出席率・カテゴリ別集計が`GET /users/me/stats`のレスポンスをそのまま表示すること
- 【表示】`attendanceRate === null`（未マーク分のみ等でゼロ除算になるケース）の場合、「-」等の代替表示になり`NaN%`や`0%`と誤表示しないこと

### OrganizingEventRow
- 【表示】「参加者{confirmedCount}/{capacity}」表示になること（画面設計仕様.md 3.1.5モックアップ、過去に`capacity`欠落で表示できない不具合があった箇所）
- 【表示】キャンセル待ちが1件以上ある場合のみ「(待機n)」の補足が付くこと、0件の場合は付かないこと
- 【遷移】「管理」ボタンクリックでイベント詳細（P-03）へ遷移すること

### UpcomingEventRow / HistoryEventRow
- 【表示】`formatEventDateTime`によるフォーマット済み日時が表示されること
- 【表示】HistoryEventRowで`attendanceStatus === null`（未マーク）の場合の表示（出席/欠席と区別できる表示になること）

---

## 8. attendance（`features/attendance/*`、P-07出席管理）

### AttendancePage
- 【状態】開催日時（`startAt`）より現在時刻が前の場合、マーク操作ボタンが`disabled`になること（`vi.useFakeTimers()`でイベント前の時刻に固定して検証）
- 【状態】開催日時を過ぎている場合、マーク操作ボタンが活性になること（同様に開催後の時刻に固定して検証）
- 【権限】「強制キャンセル」ボタンはadminのときのみ表示されること（`canForceCancel`）
- 【状態】参加者0件の場合、「参加者がいません。」の表示になること
- 【状態】取得中のローディング表示、取得失敗時のエラー表示

### AttendanceRow
- 【表示】未マーク（`attendanceStatus === null`）の場合、出席/欠席どちらのボタンも非選択状態で活性表示
- 【表示】出席マーク済みの場合、出席ボタンが選択状態（●）で表示されること
- 【表示】欠席マーク済みの場合、欠席ボタンが選択状態（●）で表示されること
- 【操作】出席/欠席ボタンクリックで`onMark(userId, status)`が呼ばれること
- 【操作】マーク済みボタンの再クリック（訂正）に確認モーダルを挟まず、即座に`onMark`が呼ばれること（画面設計仕様.md 3.1.6、誤操作リカバリ優先の方針）
- 【表示】`isMarkingDisabled=true`の場合、出席/欠席ボタンが`disabled`になること
- 【操作】「強制キャンセル」クリックで`onForceCancel(userId, name)`が呼ばれること（即APIを呼ばず、呼び出し元でのモーダル起動に委ねること）
- 【状態】`isMarkPending`かつ対象行の場合のみローディング表示になること（他の行には影響しないこと）

### ForceCancelConfirmModal（M-05）
- 【表示】期限を過ぎていても強制的にキャンセルする旨の警告文言が表示されること
- 【操作】確定で`onConfirm`が呼ばれること

---

## 9. feedbacks（`features/feedbacks/*`、P-08フィードバック投稿・P-03内レビュー一覧）

### FeedbackPage
- 【状態】投稿条件未充足（サーバー`403`相当、`onIneligible`経由）の場合、フォームの代わりに理由メッセージとP-03への戻り導線のみが表示されること（画面設計仕様.md 3.1.7）
- 【表示】自分の投稿が既にある場合（`isMine: true`の項目が存在）、フォームが投稿済み内容（`rating`/`comment`/`isAnonymous`）で初期化され、ボタン文言が「更新する」になること
- 【表示】自分の投稿が匿名投稿（`author: null`）でも、`isMine`により編集モードとして正しく検出されること（`author?.id === me.id`判定に戻すと検出できない回帰の防止、過去の不具合修正箇所）
- 【表示】未投稿の場合、フォームが空で初期化され、ボタン文言が「投稿する」になること
- 【操作】未投稿からの送信→`submitFeedback`（`POST`）が呼ばれること
- 【操作】投稿済みからの送信→`updateFeedback`（`PUT /feedbacks/:id`）が呼ばれること（`POST`ではないこと）
- 【状態】ヘッダー情報・フィードバック一覧取得中のローディング表示、取得失敗時のエラー表示

### FeedbackForm
- 【入力】評価（星）未選択→バリデーションエラー、送信されないこと
- 【入力】コメント文字数上限超過→バリデーションエラー
- 【操作】星クリックで該当の評価値が選択されること（`StarRatingInput`）
- 【操作】「匿名で投稿する」チェックボックスの状態が送信データの`isAnonymous`に反映されること

### FeedbackList（P-03内レビュー一覧）
- 【表示】非管理者向け: 匿名投稿は投稿者名の代わりに固定文言（「匿名希望」等）で表示されること
- 【表示】管理者向け: 匿名投稿でも投稿者名が表示されること（レスポンスの`author`出し分けをそのまま反映）
- 【表示】平均評価・件数がヘッダーに表示されること（非公開分は集計・一覧の両方から除外されていることをAPIレスポンスの前提としてテストする）
- 【権限】「非公開化」ボタンはadminのときのみ表示されること
- 【操作】「非公開化」クリックで確認モーダル（M-06）が開き、確定で`hideFeedback`（`POST /feedbacks/:id/hide`）が呼ばれること
- 【表示】M-06に「非公開化は取り消し不可」の旨が明記されていること

---

## 10. admin/categories（`features/admin/categories/*`、P-09カテゴリマスタ管理）

### CategoriesAdminPage
- 【表示】各カテゴリ行に「紐づくイベント数」が表示されること
- 【表示】イベント数が0件以外の場合でも削除ボタンがフロント側で無効化されないこと（画面設計仕様.md 3.1.8、可否判定は必ずサーバーに委ねる方針の確認）
- 【操作】「＋追加」クリックでカテゴリ追加モーダル（M-01、新規モード）が開くこと
- 【操作】「編集」クリックでカテゴリ編集モーダル（M-01、対象カテゴリの値で初期化）が開くこと
- 【操作】「削除」クリックで削除確認モーダル（M-03）が開くこと

### CategoryFormModal（M-01）
- 【入力】カテゴリ名未入力→バリデーションエラー、送信されないこと
- 【入力】カテゴリ名51文字以上→バリデーションエラー
- 【操作】新規モード送信→`POST /categories`が呼ばれること
- 【操作】編集モード送信→`PUT /categories/:id`が呼ばれること
- 【エラー】同名カテゴリ重複（サーバー`409`）発生時、モーダルを閉じずフィールドエラーとして表示すること（画面設計仕様.md 2章 M-01）

### DeleteCategoryConfirmModal（M-03）
- 【操作】確定で`DELETE /categories/:id`が呼ばれること
- 【エラー】紐づくイベントが存在する場合のサーバー`409`エラーメッセージがモーダル内にそのまま表示されること（画面設計仕様.md 2章 M-03、DB制約由来のエラーメッセージをそのまま表示する方針）

---

## 11. 横断的関心事

- 【共通】`apiClient`が`204 No Content`レスポンス（例: カテゴリ削除成功時）を`.json()`を呼ばずに処理できること（過去に無条件`.json()`呼び出しで例外になった不具合の再発防止、`lib/api-client.ts`）
- 【共通】register/cancel/markAttendance等の楽観的UI更新を行わず、レスポンス確定後にのみ表示が更新されること（画面設計仕様.md 3.4、`RegistrationActionButton`等で個別に検証済みの内容の横断確認）
- 【共通】イベント作成・更新・削除後、`features/events`側の一覧・詳細キャッシュも`invalidateQueries`されること（過去のキャッシュ不整合の再発防止、`features/events-form`の各ミューテーションフック）
- 【共通】`dayjs`ベースの日時表示・比較がタイムゾーン（`tz()`）を考慮していること
