# 詳細要求リスト

本ドキュメントは、[初期選定要素リスト.md](./初期選定要素リスト.md)の8カテゴリ構成に沿って、これまでの対話で
決定した設計方針を正式な仕様として整理したものである。個々の決定の比較検討過程・却下案は
[選定要素提案.md](./選定要素提案.md)、ライブラリの具体的なバージョンは[技術スタック.md](./技術スタック.md)、
DB/API/画面まで含めた設計の詳細は[MANIFEST.md](./MANIFEST.md)を参照。
短くまとめた版は[設計方針決定サマリー.md](./設計方針決定サマリー.md)を参照。

---

## 1. 全体アーキテクチャと技術スタック

- 全体構成はSPA(React + Vite) + REST API(NestJS)による2層構成とする。BFFやSSRフレームワーク(Next.js等)は採用しない。
- バックエンドはNode.js + TypeScript + NestJSとする。HTTPアダプタは`@nestjs/platform-express`(Express)を使用し、課題の絶対要件「Express」はこれをもって満たすものとする。
- データベースはPostgreSQL、ORMはPrismaとする。
- フロントエンドはReact 18 + TypeScript + Viteとする。スタイリングはTailwind CSSとする。
- 状態管理・データフェッチはTanStack Queryとする。Redux等のクライアント状態管理ライブラリは導入しない。
- フォームはReact Hook Form + Zodとする。
- バリデーションスキーマ(Zod)は`packages/shared`でフロントエンド・バックエンド共通の定義として管理する。
- リアルタイム同期(WebSocket/SSE)は導入しない。参加登録・キャンセル等の操作後は、TanStack Queryのキャッシュ無効化によって画面を最新化する。
- リポジトリ構成はモノレポ(pnpm workspace + turbo)とし、リポジトリ直下に`backend/`・`frontend/`・`packages/shared/`を配置する。
- 前プロジェクト(uniteboard)の技術スタック・共通基盤(認証・CASL・共通UI)をそのまま継承し、ドメイン固有部分(タスク管理関連)のみ新ドメイン(イベント運営)向けに置き換える。

---

## 2. コード管理とディレクトリ構成

- リポジトリはGitによる単一リポジトリとする(ローカルで`git init`済み)。納品時のGitHubへのアップロードはユーザー側で行う。
- ディレクトリ構成は前プロジェクトを踏襲する。
  - `backend/src/{common, modules, prisma}`(common=認証・認可・設定・フィルタ・ガード等の共通基盤、modules=リソースごとのモジュール)
  - `frontend/src/{features, components, router, lib}`(features=機能単位ディレクトリ、components/ui=汎用UI)
- バックエンドはレイヤードアーキテクチャ(Controller-Service-Repository相当、Prismaがrepository層を兼ねる)を採用する。
- フロントエンドはfeature-basedディレクトリ構成(`features/<domain>/{api.ts, components/, hooks/}`)を採用する。
- 共通の型・バリデーションは`packages/shared`のZodスキーマを単一の情報源とし、フロント・バックの双方からimportする。
- Prismaスキーマ(`backend/prisma/schema.prisma`)をドメインモデルの単一の情報源とする。
- コーディング規約は[CODING_STANDARDS.md](./CODING_STANDARDS.md)に**確定**（命名規則・レイヤー構成・API設計・Prisma規約・テスト規約・Git規約・環境変数管理・セキュリティガイドライン・コードレビューチェックリスト）。MANIFEST.mdの技術スタック・ディレクトリ構成に沿った内容としている。
- Lint/FormatterはESLint + Prettierを各パッケージ(`backend`/`frontend`/`packages/shared`)ごとに設定する(前プロジェクト踏襲)。
- コミット規約・ブランチ運用は本ドキュメントの対象外とし、別途決定する。

---

## 3. セキュリティ・認証・認可

- 認証はメールアドレス+パスワードによるログインのみとする。招待制は採らず、初期ユーザーは`backend/prisma/seed.ts`で作成する(登録APIは提供しない)。
- パスワードはbcrypt(コストファクタ10)でハッシュ化して保存する。平文保存は行わない。
- セッション管理はJWT(Access Token 15分・Refresh Token 7日)をhttpOnly Cookieで行う。
- Access Tokenのペイロードは`{ sub: userId }`のみとし、ロール(`role`)は含めない。認可判定のたびにDBから最新のロール・有効/無効状態(`isActive`)を取得する。これにより昇格/降格・無効化がAccess Tokenの残り有効期限に関わらず即座に反映される。
- Refresh Tokenは`/auth/refresh`実行のたびにローテーション(旧トークンを`revokedAt`で失効させ新規発行)する。DBにはハッシュ値(SHA-256)のみ保存する。
- 「失効済みRefresh Tokenの再送信を検知した場合に該当ユーザーの全セッションを強制失効させる」機能(トークン盗難対策)は今回追加する方針としたが、**未実装**。着手時に対応する。
- ロールは`member`(全社員のデフォルト)と`admin`(全イベント・全ユーザー操作可、カテゴリマスタ管理)の2段階のみとする。
- 「主催者」は独立ロールを設けず、`Event.organizerId`(作成者)とリクエスト実行者本人の一致をCASL(Ability)でその都度判定する方式とする。
- 認可はNestJSのGuard(`JwtAuthGuard` + `PoliciesGuard`) + CASLで一元的に実施する。
- 認可エラーは`403`を返し、日本語の分かりやすいメッセージを含める(詳細度を優先し、汎用メッセージへの統一は行わない)。
- CORSはOriginを環境変数で限定する。CSRF対策は`SameSite=Lax`のCookie属性で行い、CSRFトークンの追加実装は行わない。
- レート制限はログイン試行回数制限のみ実装する。API全体への一律のレート制限は導入しない。
- 監査ログはadmin操作(強制キャンセル・レビュー非公開化等)についてのみアプリケーションログ(NestJS標準Logger)に出力する。専用の監査テーブルは設けない。
- XSS対策はReactの自動エスケープに全面的に依拠する。`dangerouslySetInnerHTML`は使用せず、イベント説明文・フィードバックコメント・タグ名はすべてプレーンテキストとして描画する。HTMLサニタイズライブラリ(sanitize-html等)は導入しない。将来Markdown/リッチテキスト表示を追加する場合は、その時点でサニタイズ処理の追加を必須とする。
- SQLインジェクション対策はPrismaのパラメータ化クエリに全面的に依拠する。生SQL(`$queryRawUnsafe`等)は使用しない。
- パストラバーサル対策: 現状の要件にファイルアップロード・静的ファイル配信機能が存在しないため対応不要とする。将来ファイル添付機能を追加する場合に別途設計する。
- `helmet`によるHTTPセキュリティヘッダーの付与を推奨する(本格的なContent-Security-Policyの設計は見送る)。
- 通信の暗号化(HTTPS化)はデプロイ環境が今回のスコープ外(GitHubへのコードアップロードのみ)のため対象外とする。

---

## 4. データ管理と主要なビジネスルール

- 要件定義の推奨(Should)機能のうち、**キャンセル待ち＋自動繰り上げ**と**フィードバック(星評価＋コメント)は実装対象に含める**。通知システムのみ実装対象から除外する(5章参照)。
- 主要エンティティは`User` / `Category` / `Tag` / `EventTag` / `Event` / `Registration` / `PromotionHistory` / `Feedback`の8テーブルとする(詳細は[MANIFEST.md](./MANIFEST.md) 5章)。
- 各イベントは1つの`Category`を必ず持つ(初期値: 勉強会/懇親会/講演会/研修/その他)。複数の`Tag`を任意で持てる(多対多、中間テーブル`EventTag`)。
- **カテゴリ削除**: 紐づく`Event`が1件でも存在する場合は削除を拒否する(`409`、DBの外部キー制約`onDelete: Restrict`を活用)。論理削除・別カテゴリへの自動移動は採用しない。
- **イベント削除**: 論理削除(`Event.deletedAt`)とする。物理削除は行わない(参加履歴・出席率・フィードバックの参照整合性を保つため)。
- **イベント作成**: 過去日時(`startAt <= now`)での作成は禁止する(バリデーションエラー)。
- **イベント編集**: 開催日時(`startAt`)を変更する際、既に`CONFIRMED`の参加登録者が1人以上いる場合、レスポンスに`hasRegisteredParticipants: true`を含めフロントで警告表示する(通知送信は行わない)。
- **参加登録**: 定員(`Event.capacity`)に達したイベントへの登録は自動的にキャンセル待ち(`Registration.status = WAITLISTED`)として扱う。登録締切(`registrationDeadline`、未設定時は`startAt`をデフォルトとする)を過ぎたら登録不可。過去のイベントには登録不可。同一ユーザーの二重登録は`(eventId, userId)`の一意制約で防止する。
- **キャンセル**: キャンセル可能期限(`cancellationDeadline`、未設定時は`startAt`をデフォルトとする)を過ぎたら通常キャンセル不可。adminは期限を無視した強制キャンセルが可能。
- **主催者の扱い**: 主催者は暗黙的に参加確定として扱い、`Registration`レコードを作らない。マイページの参加履歴には主催イベントも合算して表示する。
- **キャンセル待ちの順序**: `position`(Float)で管理し、新規の待機登録は常に列の末尾(現在の最大`position` + 1)に追加するFIFO方式とする。待機解除時の全件再採番は行わない。
- **繰り上げ**: 誰かが`CONFIRMED`をキャンセルすると、当該イベントの`WAITLISTED`のうち`position`最小の者を自動的に`CONFIRMED`へ繰り上げる。イベント行を`SELECT ... FOR UPDATE`でロックした上で「キャンセル削除→先頭繰り上げ→履歴insert」を1トランザクションで実行する。繰り上げ履歴は`PromotionHistory`に保存する。
- **出席マーク**: 開催日時(`startAt`)に達する前のマークは禁止する。マーク後の変更(誤操作リカバリ)は許可する。
- **出席率**: 出席回数 ÷ (出席回数 + 欠席回数)。未マーク(`AttendanceStatus = null`)は分母から除外する。
- **フィードバック**: 開催終了済み(`endAt`、未設定時は`startAt`を終了とみなす) かつ 投稿者が`ATTENDED`としてマークされている場合のみ投稿可能。`(eventId, userId)`の一意制約により1人1件に制限する(編集は可能)。匿名投稿(`isAnonymous`)は投稿者情報を一般ユーザーに非表示、adminには表示する。非公開化(`isHidden`)されたレビューは平均評価・一覧集計から除外する。物理削除は行わない。
- **タグの正規化**: 完全自由入力のまま保存し、登録時にtrim+小文字化する程度に留める。タグマスタ化・エイリアス機構は導入しない。
- **日時・タイムゾーン**: DBはUTC(`@db.Timestamptz`)で保存し、表示はdayjsでJSTに変換する。
- 日時に依存するバリデーション(過去日時禁止、締切判定、出席マーク可否等)はすべてアプリケーション層(Service)で判定する。DBのCHECK制約による担保は行わない(現在時刻比較をDB制約に持たせるとアプリ層のテストがしづらくなるため)。

---

## 5. 通知システム

- 今回のスコープでは通知機能(メール送信・アプリ内通知)を**実装しない**。要件でも「通知送信までは不要(ログ出力/ステータス表示で可)」と明記されているため。
- BullMQ/Redis/Nodemailer等の通知基盤は導入しない。
- 将来の通知機能拡張に備え、`PromotionHistory`(繰り上げ履歴: いつ・誰が・誰の代わりに繰り上げられたか)テーブルのみ保持する。

---

## 6. API設計と認可方針

- APIスタイルはREST(GraphQL等は採用しない)とする。
- リソース設計は`/events`, `/events/:id/registrations`, `/events/:id/feedbacks`, `/categories`等、イベント配下にネストする素直なREST設計とする。
- 状態遷移・副作用を伴う操作は専用アクションエンドポイントとしてPOSTで表現する(例: `POST /events/:id/register`, `POST /events/:id/cancel`)。
- **参加登録・キャンセルはAPIとして2アクションのみ**とする。満席時のキャンセル待ちは`register`が定員超過を検知して自動的に`WAITLISTED`として登録し、待機解除も`cancel`と同一操作とする(キャンセル待ち専用のエンドポイントは設けない)。
- イベント詳細・一覧のレスポンスには、ログインユーザー視点の`registrationState`(例: `NOT_REGISTERED`/`CONFIRMED`/`WAITLISTED`/`ORGANIZER`/`CLOSED`)をサーバー側で計算して含める。参加登録ボタンの出し分けロジックはフロントに持たせない。
- 認可はJwtAuthGuard + PoliciesGuard(CASL)で一元的に実施する(既存実装を継続)。
- エラーレスポンスは`{ success: false, error: { code, message } }`で統一する(既存実装を継続)。
- ページネーションは当面省略する(社内規模であれば一覧を全件返却しても問題ない想定)。将来必要になればオフセット方式を追加する。
- APIバージョニングは導入しない。
- API仕様は[MANIFEST.md](./MANIFEST.md) 6章に、全28エンドポイントのリソース一覧＋詳細なリクエスト/レスポンス仕様（フィールド・認可・エラーコード）として記載済み。

---

## 7. 画面構成とUI/UX

- UIはTailwind CSS + 自前コンポーネント(Button/Modal/Toast/ConfirmDialog等、前プロジェクトから継承済み)を継続する。MUI/Chakra UI等の外部UIライブラリは導入しない。
- ルーティングはReact Routerによるクライアントサイドルーティングとする。
- 画面構成は[MANIFEST.md](./MANIFEST.md) 4章の通り、全10画面(ログイン/イベント一覧/イベント詳細/イベント作成/イベント編集/マイページ/出席管理/フィードバック投稿/カテゴリマスタ管理/404)とする。
- 参加登録ボタンの状態(空席あり/満席/締切済み/キャンセル待ち中/主催者本人等)は、バックエンドが計算した`registrationState`をそのまま表示に反映する(6章参照)。フロントで定員・締切等を再計算するロジックは持たせない。
- マイページの集計情報(累計参加数・出席率・カテゴリ別傾向)はリアルタイム集計(都度クエリ)とし、事前計算(非正規化キャッシュ)は行わない。
- 出席管理画面の一括操作(全員出席にする等)はMVPでは提供しない(1人ずつのマークのみ)。
- レスポンシブ対応はPCメインとし、Tailwindのbreakpointによる最低限の崩れ防止のみ行う。モバイル最適化への追加投資は行わない。
- ローディング・エラー・空状態(イベント0件等)のUI設計方針は画面間で統一する。
- admin向け画面は別アプリとして分離せず、同一アプリ内でロール制御(既存`AdminRoute`)によって出し分ける。
- 多言語対応(i18n)は行わない。日本語のみとする。
- アクセシビリティは特別な追加対応(WCAG準拠レベル等)を設けず、標準的なセマンティックHTML・フォームラベル付けの範囲に留める。

---

## 8. インフラと開発環境

- ローカル開発環境はDocker Compose(`db`/`backend`/`frontend`の3サービス)で構築する。前プロジェクトにあったRedis/Mailpitサービスは、通知機能を見送ったため含めない。
- データベースのマイグレーションはPrisma Migrateで行う。
- Seedデータ(`backend/prisma/seed.ts`)で初期admin(環境変数`SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD`)・サンプルmember・カテゴリマスタ初期値(勉強会/懇親会/講演会/研修/その他)を投入する。
- 環境変数は`.env`(`.gitignore`対象)+`.env.example`で管理する。JWT秘密鍵はランダムな32文字以上の文字列を必須とする。
- CI(自動テスト・Lint)は導入しない。提出がGitHubへのコードアップロードのみで、デプロイ・ホスティングが不要と確定したため。
- 提出形態は「`docker compose up`で一式(db/backend/frontend)が起動すること」を最終確認基準とする。デプロイ済み環境の提出は不要。
- ロギングはNestJS標準のLoggerを使用し、警告ログ(開催日時変更時の警告等)は該当Service内で出力する。外部ログ基盤(Sentry/Datadog等)は導入しない。
- Node.js/pnpmのバージョンは`package.json`の`packageManager`フィールド(pnpm 9.15.0)で固定する。
- テスト戦略はバックエンド=Jest(ユニット)、フロントエンド=Vitest(ユニット)+ Playwright(E2E)とする(前プロジェクト踏襲)。
